apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-simple-app.fullname" . }}
  labels:
    {{- include "my-simple-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-simple-app.selectorLabels" . | nindent 4 }}
